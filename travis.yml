language: csharp
solution: src/CConsoleAction.sln
install:
  - nuget restore src/ConsoleAction.sln
  - nuget install xunit.runners -Version 1.9.2 -OutputDirectory testrunner
script:
  - msbuild /p:Configuration=Release src/ConsoleAction.sln
